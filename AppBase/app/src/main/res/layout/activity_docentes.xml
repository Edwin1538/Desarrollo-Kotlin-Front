<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#F8F9FA"
        tools:context=".DocentesActivity">

    <!-- Toolbar con título -->
    <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="0dp"
            android:layout_height="?attr/actionBarSize"
            android:background="@android:color/white"
            android:elevation="4dp"
            app:title="Gestión de Docentes"
            app:titleTextColor="@android:color/black"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botones con estilo FAB agrupados -->

    <!-- Chips horizontales para acciones -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/CrearDocente"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:src="@android:drawable/ic_input_add"
            android:onClick="crearDocente"
            app:backgroundTint="#1DA1F2"
            app:tint="@android:color/white"
            app:layout_constraintTop_toBottomOf="@+id/toolbar"
            app:layout_constraintEnd_toEndOf="parent" android:layout_marginTop="8dp" android:layout_marginEnd="28dp"/>
    <HorizontalScrollView
            android:id="@+id/chipsContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="80dp"
            android:scrollbars="none"
            android:paddingEnd="16dp"
            android:clipToPadding="false"
            app:layout_constraintTop_toBottomOf="@+id/toolbar"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/CrearDocente">

        <com.google.android.material.chip.ChipGroup
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleSelection="false"
                app:chipSpacing="8dp">

            <com.google.android.material.chip.Chip
                    android:id="@+id/chipMostrar"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Mostrar"
                    android:onClick="mostrardocentes"
                    app:chipBackgroundColor="#E3F2FD"
                    app:chipStrokeColor="#1976D2"
                    app:chipStrokeWidth="1dp"
                    android:textColor="#1976D2" />

            <com.google.android.material.chip.Chip
                    android:id="@+id/chipEditar"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Editar"
                    android:onClick="editarDocente"
                    app:chipBackgroundColor="#FFF3E0"
                    app:chipStrokeColor="#F57C00"
                    app:chipStrokeWidth="1dp"
                    android:textColor="#F57C00" />

            <com.google.android.material.chip.Chip
                    android:id="@+id/chipEliminar"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Eliminar"
                    android:onClick="eliminarDocente"
                    app:chipBackgroundColor="#FFEBEE"
                    app:chipStrokeColor="#D32F2F"
                    app:chipStrokeWidth="1dp"
                    android:textColor="#D32F2F" />

        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <!-- Lista de docentes con estilo moderno -->
    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/listDocentes"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            android:clipToPadding="false"
            android:paddingTop="8dp"
            android:paddingBottom="80dp"
            android:scrollbars="none"
            android:overScrollMode="never"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/chipsContainer"
            app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Botón volver flotante -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/buttonvolver"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="Volver"
            android:onClick="volverpag"
            app:icon="@android:drawable/ic_menu_revert"
            app:backgroundTint="#6C757D"
            app:iconTint="@android:color/white"
            android:textColor="@android:color/white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>